# PyTorch C++ and Go example

## Create ResNet model

Go into `model`. And run `save_model.py` to create a `traced_resnet_model.pt`.

## Install C++ PyTorch

Go into `library/libs/` and follow instructions.

## Build

Just run `make`